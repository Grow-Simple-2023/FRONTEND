import React from "react";
import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const RiderScreen = () => {
  return (
    <SafeAreaView>
      <Text>RiderScreen</Text>
    </SafeAreaView>
  );
};

export default RiderScreen;
